<!DOCTYPE html>
<html lang="en" xmlns:th="https://thymeleaf.org">
<head>
<meta charset="ISO-8859-1">
<title>Library</title>
</head>
<body>
	<h2>Book Shelf</h2>
	<a th:href="@{addbook}" ><button>Add Book</button></a>
	<table>
		<thead>
			<tr>
				<th>Id</th>
				<th>Title</th>
				<th>Author</th>
				<th>Genre</th>
				<th>Price</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			<tr th:each="book : ${booklist}">
				<td th:text="${book.id}"></td>
				<td th:text="${book.title}"></td>
				<td th:text="${book.author}"></td>
				<td th:text="${book.genre}"></td>
				<td th:text="${book.price}"></td>
				<td><a th:href="@{/updatebook/{id}(id=${book.id})}">Update</a>
				<td><a th:href="@{/deletebook/{id}(id=${book.id})}">Delete</a>
			</tr>
		</tbody>
	</table>
</body>
</html>